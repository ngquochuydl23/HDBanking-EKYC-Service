version: '3.4'
services:
  hd-wallet.ocr.service:
    image: hd-wallet.ocr.service
    container_name: hd-wallet.ocr.service
    build:
      context: ./HD.Wallet.OCR.Service
      dockerfile: Dockerfile
    volumes:
      - ./HD.Wallet.OCR.Service/uploads:/app/uploads  
    ports:
      - 8000:8000
    restart: always

